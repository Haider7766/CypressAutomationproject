{
  "name": "cypressautomation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf cypress/reports && rimraf cypress/videos && rimraf cypress/screenshots",
    "cy:run": "npm run clean && npx cypress run",
    "cy:run:report": "npm run clean && npx cypress run --reporter cypress-multi-reporters --reporter-options configFile=reporter-config.json",
    "cy:open": "npx cypress open",
    "cy:run:headed": "npm run clean && npx cypress run --headed",
    "report:merge": "mochawesome-merge \"cypress/reports/mochawesome/*.json\" > cypress/reports/mochawesome/merged-report.json",
    "report:generate": "marge cypress/reports/mochawesome/merged-report.json --reportDir cypress/reports/mochawesome --inline --charts --reportPageTitle 'AMP Cypress Test Report' --reportFilename index.html",
    "report:full": "npm run report:merge && npm run report:generate",
    "test": "npm run cy:run:report && npm run report:full",
    "test:ci": "npm run clean && npx cypress run --reporter cypress-multi-reporters --reporter-options configFile=reporter-config.json"
  },
  "keywords": [
    "cypress",
    "automation",
    "testing",
    "e2e"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "13.17.0",
    "cypress-mochawesome-reporter": "^4.0.0",
    "cypress-multi-reporters": "^2.0.5",
    "mocha-junit-reporter": "^2.2.1",
    "mochawesome": "^7.1.4",
    "mochawesome-merge": "^5.1.0",
    "mochawesome-report-generator": "^6.3.2",
    "rimraf": "^5.0.10"
  },
  "dependencies": {
    "crypto-js": "^4.2.0",
    "imap-simple": "^5.1.0",
    "mailparser": "^3.9.0",
    "nodemailer": "^7.0.10"
  }
}
